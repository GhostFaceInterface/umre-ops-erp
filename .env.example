SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional. Used only when scripts/index_codebase.py --init-db is used.
SUPABASE_DB_URL=

# Production target model for the AI Code Intelligence System.
CODE_INTEL_MODEL_NAME=BAAI/bge-code-v1
CODE_INTEL_EMBEDDING_DIMENSION=1536

# Optional. Use float16 on Apple Silicon/GPU if model loading is too memory-heavy.
# CODE_INTEL_TORCH_DTYPE=float16

# Optional Hugging Face token for higher rate limits.
HF_TOKEN=
